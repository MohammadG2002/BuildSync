/**
 * Async Handler Wrapper
 * Wraps async route handlers to catch errors and pass to error middleware
 */

export { asyncHandler } from "./asyncHandler.js";
export { asyncHandlers } from "./asyncHandlers.js";
